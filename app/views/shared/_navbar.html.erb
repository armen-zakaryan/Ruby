<nav class="navbar navbar-inverse h-navbar">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <img class="h-logo-img" src="/images/logo.png">
      <a class="navbar-brand" href="/">Global Hotel Network</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if session[:username] %>
            <li class="h-login-info">Welcome!!! <span><%= session[:username] %></span></li>
        <% end %>
        <li><a href="/">TODO</a></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Menu <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to 'Hotels', controller: 'hotels' %></li>
            <li><%= link_to 'Articles', controller: 'articles' %></li>
            <li role="separator" class="divider"></li>
            <% if session[:username] %>
                <li><%= link_to("Logout",:controller => 'api/v1/auth', :action => 'logout') %></li>
            <% else session[:username] %>
                <li><%= link_to("Login", :controller => 'access', :action => 'login') %></li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>