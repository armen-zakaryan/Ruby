<tr>
  <td><%= comment.commenter %></td>
  <td><%= comment.body %></td>
  <td><div class="trash-container"><%= link_to '', article_comment_path(comment[:article_id], comment), class: 'glyphicon glyphicon-trash',
                                               method: :delete,
                                               data: { confirm: 'Are you sure?' } %></div></td>
</tr>